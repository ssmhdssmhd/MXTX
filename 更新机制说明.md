# 更新机制说明（老版本如何更新）

> 对应实现：`update.js`，入口 `updateSource()`。

## 版本状态（更新时间：2026-08-20）

| 分支 | 类型 | 当前版本 | 最新发布 | 发布时间 |
|------|------|---------|---------|---------|
| `main` | 稳定版（stable） | v1.2.0 | `v1.2.0` | 2026-08-18 |
| `cs1` | 先行版（beta） | v2.6.3 | `v2.6.3-cs1` | 2026-08-20 |

- **main（稳定版）**：走 `main` 分支 / 无后缀 Release 资产，稳定可用，推荐生产部署。
- **cs1（先行版）**：走 `cs1` 分支 / `-cs1` 后缀 Release 资产，包含 v2.6.1 不再自动重启、v2.6.2 自动同步依赖、v2.6.3 后台误报修复等最新内容。

老版本（旧部署）执行「在线更新」时，根据部署方式自动分流，**只走两条路径**：

```
updateSource()
   │
   ├─ 目录是 git 仓库（存在 .git）？
   │     是 ──▶ 路径一：git 拉取（updateSourceViaGit）
   │     否 ──▶ 路径二：Release 源码包下载（updateSourceViaZip）
   │
   └─ 决定走哪条分支：
         update-source 读取 update-config.json：
            stable → main 分支（稳定版，资产无后缀）
            beta   → cs1 分支（先行版，资产带 -cs1 后缀）
         默认（配置文件缺失时）→ beta（cs1）
```

---

## 路径一：git 拉取（git 仓库部署）

适用于用 `git clone` 部署的服务。直接拉取 GitHub 对应分支代码。

流程：

1. `git fetch origin <branch>` 拉取远端目标分支（如 `cs1`）。
2. `git show origin/<branch>:package.json` 读取远端版本号，
   用 `compareVersions` 与当前版本比较，**不递增则跳过更新**。
3. 备份 `SOURCE_FILES`（node.js / api.php / package.json / admin.html / update.js / README / CHANGELOG 等 11 个文件），
   单独备份受保护文件 `.env`、`.user.ini`。
4. 若当前本地分支 ≠ 目标分支，`git checkout <branch>` 切换。
5. `git reset --hard origin/<branch>` 强制同步远端最新代码。
6. `node --check node.js` 语法验证，**失败自动回滚**到备份。
7. 恢复受保护文件（.env 等本地配置不被覆盖）。

特点：

- 直接拿分支最新代码，**不依赖 Release**，分支推了即可更新。
- 仓库公开时可匿名拉取，无需配置 token。
- 只操作目标分支（cs1 / main），不动其他分支。

---

## 路径二：Release 源码包下载（zip 解压部署）

适用于非 git 部署（下载 Release zip 上传服务器的情况）。从 GitHub Releases 下载源码包解压替换。

流程：

1. `getLatestRelease()` 调 GitHub Releases API（`?per_page=30`），
   按分支过滤 tag：cs1 分支只认含 `-cs1` 的 tag（如 `v2.6.0-cs1`）。
   API 限流（403/429）时回退 `releases.atom` Feed 解析，不受限流影响。
2. `findAsset(release, 'source')` 按命名约定找源码包：
   - 稳定版：`super-sniffer-source_<version>.zip`
   - 先行版：`super-sniffer-source_<version>-cs1.zip`
3. 版本递增保护：`normalizeVersion` 去掉 `v` 前缀与 `-cs1` 后缀后比较，
   **新版本 ≤ 当前版本则跳过更新**（防降级/重复覆盖）。
4. `downloadFile` 下载（最多重试 3 次、指数退避、Content-Length 完整性校验）。
5. `extractZip` 解压（unzip，缺失时回退 python3 -m zipfile）。
6. 备份 → 替换 `SOURCE_FILES` → `node --check node.js` 语法验证 → 失败回滚。

特点：

- 依赖 GitHub **Release 发布**：只有发布了对应版本（如 `v2.6.0-cs1`）并挂上源码包资产，
  老版本才能更新到该版本；**只推分支不发布 Release，zip 部署永远更新不到**。
- 资产命名必须遵循约定，否则 `findAsset` 找不到。

---

## 更新源与资产命名约定

| 更新源 | 分支 | Release tag 示例 | 源码包资产名 |
|--------|------|------------------|--------------|
| 稳定版 | `main` | `v2.6.0` | `super-sniffer-source_2.6.0.zip` |
| 先行版 | `cs1`  | `v2.6.0-cs1` | `super-sniffer-source_2.6.0-cs1.zip` |

- 更新源配置文件：`update-config.json`（本地生成，不随源码覆盖）。
- 受保护文件：`.env`、`.user.ini`（更新时保留本地版本）。
- 更新后验证失败会自动回滚，保证服务不中断。
